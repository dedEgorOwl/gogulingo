import { useState } from 'react';

import styles from './Tasks.module.scss';

function Tasks({langPackage}) {

    return (
        <div className={styles.wrapper}>
            <div className={styles.container}></div>
        </div>
    )
};